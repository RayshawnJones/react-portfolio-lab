import React from "react";
import ReactDOM from "react-dom";
import Portfolio from "./portfolio"; // Corrected the casing for the file name

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Portfolio />
  </React.StrictMode>
);
